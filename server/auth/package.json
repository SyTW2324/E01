{
  "name": "auth",
  "version": "0.1.0",
  "main": "dist/index.js",
  "license": "EUPL",
  "private": true,
  "scripts": {
    "build": "rm -Rf dist && yarn tsc",
    "once": "yarn run build && node dist/index.js"
  },
  "dependencies": {
    "@types/bcrypt": "^5.0.2",
    "bcrypt": "^5.1.1",
    "express": "^4.18.2",
    "mongodb": "^6.3.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/mongodb": "^4.0.7",
    "typescript": "^5.3.2"
  }
}
